/**
 * Module dependencies
 */

import { Request, Response } from 'express';


/**
 * v1 - userController - login (01)
 */
export const login = async(req: Request, res: Response) => {
    return res.status(200).send();
};